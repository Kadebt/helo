const initialState = {
    username: '',
    id: 0,
    profilepic: '',
    
}
export default function (state = initialState, action){
    switch (action.type) {
        default:
            return state
    }
}